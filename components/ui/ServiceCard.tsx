import Link from 'next/link'
export default function ServiceCard({code,name,about,tier}:{code:string,name:string,about:string,tier:'free'|'standard'|'pro'|'balance_only'}){const badge=tier==='free'?'Доступно':tier==='standard'?'Standard':tier==='pro'?'Pro':'Баланс';return(<Link href={`/service/${code}`} className='card block hover:bg-white/10 transition'><div className='flex items-center gap-3'>{/* eslint-disable-next-line @next/next/no-img-element */}<img src={`/icons/brand/${code}.svg`} alt='' className='w-9 h-9 rounded-xl'/><div className='flex-1'><div className='text-sm font-semibold'>{name}</div><div className='text-xs text-slate-400 line-clamp-1'>{about}</div></div><span className='badge'>{badge}</span></div></Link>)}
